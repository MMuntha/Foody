import {StyleSheet} from 'react-native';

const globalTemplateStyle = StyleSheet.create({
  container: {
    padding: 10,
  },
  organismView: {
    marginBottom: 5,
    marginTop: 5,
  },
});

export default globalTemplateStyle;
